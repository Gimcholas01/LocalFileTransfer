This flask server is created to allow users on the same network to be able to send files to each other

Server setup:
pip install Flask
pip install qrcode
python app.py

Client connection / upload:
Get server IP
Alternatively, setup local DNS